<h1>Search Results for "<%= query %>"</h1>

<% if (message) { %>
    <p><%= message %></p>
<% } else { %>
    <ul>
        <% products.forEach(product => { %>
            <li><%= product.product %> </li>
        <% }); %>
    </ul>
<% } %>

