<%- include('partials/header', { title: title }) %>

<section>
<style>
  /* Ensure the parent container doesn't overflow */
#mywish {
  overflow: hidden;
  padding: 15px;
}

/* Adjust the card and row styling */
.card {
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  border-radius: 8px;
}

.row {
  margin-left: 0;
  margin-right: 0;
}

.col-sm-6, .col-md-4 {
  padding-left: 0;
  padding-right: 0;
}

/* Media query for small devices */
@media (max-width: 576px) {
  .col-sm-6 {
    flex: 0 0 100%;
    max-width: 100%;
  }
  #mywish {
  overflow: hidden;
  padding: 15px;
}

/* Adjust the card and row styling */
.card {
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  border-radius: 8px;
}

.row {
  margin-left: 0;
  margin-right: 0;
}

.col-sm-6, .col-md-4 {
  padding-left: 0;
  padding-right: 0;
}
}

/* Media query for larger devices */
@media (min-width: 991px) {
  .col-md-4 {
    flex: 0 0 33.3333%;
    max-width: 33.3333%;
  }
  #mywish {
  overflow: hidden;
  padding: 15px;
}

/* Adjust the card and row styling */
.card {
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  border-radius: 8px;
}

.row {
  margin-left: 0;
  margin-right: 0;
}

.col-sm-6, .col-md-4 {
  padding-left: 0;
  padding-right: 0;
}
}
@media (min-width: 1200px) {
  .col-lg-2 {
    flex: 0 0 16.6667%;
    max-width: 16.6667%;
  }
}
</style>
  
  <% if (favourite.length > 0) { %>
    <div id="mywish">
      <p class="mt-3 ms-3">
        <strong>Favourite</strong> 
        <span style="float: right;">
          <a style="text-decoration: none;" href="/remove-cartall">Remove All</a>
        </span>
      </p>
      <div class="row gx-3">
        <% favourite.forEach((fav) => { %>
          <div class=" col-6 col-lg-2 mb-3">
            <div class="card h-100">
              <img src="<%= fav.imagePath1 %>" alt="<%= fav.product %>" class="img-fluid card-img-top">
              <div class="card-body">
                <p class="card-title"><%= fav.model %></p>
                <p class="card-text">₹<%= fav.price %></p>
                <a href="/deleted-favourite/<%= fav._id %>" class="btn btn-danger">Remove</a>
              </div>
            </div>
          </div>
        <% }); %>
      </div>
    </div>
  <% } else { %>
    <div class="d-flex justify-content-center">
      <img src="/public/1635918fab6854e489723a301619b7b2.jpg" alt="No Favourites" class="img-fluid">
    </div>
  <% } %>
</section>

